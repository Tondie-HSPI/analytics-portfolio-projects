# Data Note

The Hellmann's source data and ROI workbook are omitted from this public portfolio version. The analysis can be rerun locally if compatible files are placed in a local `data/` folder and the paths in the R Markdown file are adjusted.
