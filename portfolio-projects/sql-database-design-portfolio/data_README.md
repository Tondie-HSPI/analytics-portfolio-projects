# Data Note

The original course-provided databases (operational safety data, expense records, and the Sakila sample database) are omitted from this public portfolio version. `portfolio_queries.sql` documents the schema design, transformations, and reporting queries, and can be rerun locally against a compatible MySQL instance loaded with equivalent table structures.
