# Data Note

This project uses a provided demand/elasticity system rather than a private dataset, so no source data file is required. All inputs (the elasticity matrix and price scenarios) are defined directly in the R Markdown file.
